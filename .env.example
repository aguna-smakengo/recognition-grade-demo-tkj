# AWS credentials are read from AWS CLI (~/.aws/credentials) first.
# Only set these if you don't have AWS CLI configured:
# AWS_ACCESS_KEY_ID=your_key
# AWS_SECRET_ACCESS_KEY=your_secret
# AWS_SESSION_TOKEN=your_session_token_if_using_sso
# AWS_REGION=ap-southeast-1

# Optional: customize table/collection names
DYNAMODB_TABLE=StudentGrades
REKOGNITION_COLLECTION=student-faces
